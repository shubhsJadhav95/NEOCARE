apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: neocare
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: costspike-backend-deploy
        image: shubhsjadhav95/neocare-backend:v3
        ports:
        - containerPort: 5000
        env:
        
